ktor {
    deployment {
        port = 8080
        host = "0.0.0.0"
    }
    application {
        modules = [ ed.unicoach.rest.ApplicationKt.module ]
    }
}

jwt {
    secret = "test_secret"
    issuer = "http://test/"
}

database {
    url = "jdbc:postgresql://localhost:5432/unicoach-test"
    url = ${?DATABASE_URL}
    user = "postgres"
    password = "password"
    maximumPoolSize = 2
}
