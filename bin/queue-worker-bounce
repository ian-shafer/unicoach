#!/usr/bin/env bash
source "$(dirname "$0")/common"

[ "$#" -gt 0 ] && fatal -s "$EXIT_UNEXPECTED_ARG" "queue-worker-bounce accepts no arguments: $*"

"$PROJECT_ROOT/bin/queue-worker-down"
"$PROJECT_ROOT/bin/queue-worker-up"
