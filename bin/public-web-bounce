#!/usr/bin/env bash
source "$(dirname "$0")/common"

[ "$#" -gt 0 ] && fatal -s "$EXIT_UNEXPECTED_ARG" "public-web-bounce accepts no arguments: $*"

"$PROJECT_ROOT/bin/public-web-down"
"$PROJECT_ROOT/bin/public-web-up"
