#!/usr/bin/env bash
source "$(dirname "$0")/common"

[ "$#" -gt 0 ] && fatal -s "$EXIT_UNEXPECTED_ARG" "postgres-bounce accepts no arguments: $*"

"$PROJECT_ROOT/bin/postgres-down"
"$PROJECT_ROOT/bin/postgres-up"
