cron {
    # Establish the root block. The scheduler's one-minute tick interval is a
    # code default (PeriodicScheduler.tickInterval); per-job schedules live in
    # the periodic_jobs table, not here.
}
