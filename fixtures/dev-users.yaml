# Canned dev world for bin/state-apply (RFC 138). Living documentation of the
# world-file format and the smoke input for manual use:
#   nix develop -c bin/state-apply -f fixtures/dev-users.yaml
users:
  - email: ian@uni.coach
    password: Test1234
    name: Ian
    verified: true
    admin: true
